# Erforderliche Python-Pakete für das Projekt

numpy>=1.21.0  # Für numerische Operationen und Array-Verarbeitung
opencv-python>=4.5.3  # Für Computer Vision Aufgaben (Bildverarbeitung, Video)
scikit-learn>=0.24.2  # Für maschinelles Lernen (Modelle wie SVM, Random Forest, KNN, Decision Tree)
scikit-image>=0.18.2  # Für Bildverarbeitung und Feature-Extraktion (z.B. HOG)
matplotlib>=3.4.3  # Für das Erstellen von Diagrammen und Visualisierungen
pandas>=1.3.3  # Für Datenanalyse und -manipulation (wird hier möglicherweise nicht direkt verwendet, aber oft in ML-Projekten)
pillow>=8.3.2  # Für Bildverarbeitung (Alternative zu OpenCV in manchen Fällen)
joblib>=1.1.0  # Zum Speichern und Laden von Python-Objekten (z.B. Scikit-learn Modelle)
torch>=2.0.0  # Hauptbibliothek für das Deep Learning Modell (PyTorch)
torchvision>=0.15.0  # Für Computer Vision Aufgaben mit PyTorch (Datensätze, Modelle, Transformationen)
mediapipe>=0.10.0  # Für die Erkennung von Hand-Landmarken
tqdm>=4.65.0  # Für Fortschrittsbalken (z.B. während des Trainings) 